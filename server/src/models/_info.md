Represents database entities (as types) and data-access logic.

### Example files:

- user.ts

### Example code:

```js
export interface User {
    id: number;
    name: string;
    email: string;
}
```
