Contains files served exactly as they are.
favicon.ico, robots.txt, manifest.json,
Files users download, Very large static files, Images whose names/paths must remain fixed

### Example files:

- favicon.ico
- robots.txt
- images/

### In HTML:

```html
<img src="/logo.svg" />
```
