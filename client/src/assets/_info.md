Static assets imported into React.
These files are part of the application code.
Logos, Icons, Images used by components, Background images, Fonts imported by CSS

### Example files:

- logo.svg
- hero.jpg

### In JSX:

```jsx
import logo from "../assets/logo.svg";

<img src={logo} />;
```
